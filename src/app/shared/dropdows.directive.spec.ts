import { DropdowsDirective } from './dropdows.directive';

describe('DropdowsDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdowsDirective();
    expect(directive).toBeTruthy();
  });
});
